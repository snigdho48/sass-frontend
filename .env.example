# In dev, leave VITE_API_URL unset so the app uses /api and the Vite proxy forwards to the backend (no CORS).
# Only set this if you need to hit the backend directly (backend must allow CORS for your frontend origin).
# VITE_API_URL=http://127.0.0.1:8000/api
